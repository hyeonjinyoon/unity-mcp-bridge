{
  "name": "unity-mcp-bridge-server",
  "version": "1.0.0",
  "description": "MCP server that bridges Claude Code/Desktop to Unity Editor via HTTP.",
  "type": "module",
  "scripts": {
    "build": "tsc",
    "start": "node build/index.js",
    "start:build": "tsc && node build/index.js"
  },
  "dependencies": {
    "@modelcontextprotocol/sdk": "^1.12.1",
    "zod": "^3.24.4"
  },
  "devDependencies": {
    "@types/node": "^22.0.0",
    "typescript": "^5.7.0"
  }
}
